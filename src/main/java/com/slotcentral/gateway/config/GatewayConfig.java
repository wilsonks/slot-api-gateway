package com.slotcentral.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Gateway configuration placeholder.
 *
 * <p>Routes are defined declaratively in {@code application.yml} under
 * {@code spring.cloud.gateway.mvc.routes}. Add {@code RouteLocator} beans here
 * if programmatic route registration is needed in the future (e.g., dynamic
 * routes loaded from the slot-config-service).
 */
@Configuration
public class GatewayConfig {
}
